# snake-db

A minimal RESP2 Redis-compatible server built from scratch in Python.

## Run

```bash
uv run python -m snake_db          # default port 6379
SNAKE_DB_PORT=6399 uv run python -m snake_db
```
